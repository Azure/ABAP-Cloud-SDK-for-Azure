INTERFACE zif_az_token_credential  PUBLIC .

 METHODS get_token
    IMPORTING
      iv_scope TYPE string
    RETURNING
      VALUE(ro_token) TYPE REF TO zcl_az_access_token
    RAISING
      zcx_az_auth_error.

ENDINTERFACE.